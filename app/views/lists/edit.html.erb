<h1>編集問題</h1>

<%= form_with model: @list, url: update_list_path(@list.id), methhod: :patch do |f| %>

 <h4>タイトル</h4>
 <%= f.text_field :title %>

 <h4>本文</h4>
 <%= f.text_area :body %>

 <%= f.submit '保存' %>

<%end%>
