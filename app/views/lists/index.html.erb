<div　class="list-wrap">
 <% @list.each do |list| %>
 <div class="list-box">
  <% if list.image.attached? %>
   <%=image_tag list.image, size: "200x200" %>
  <% else %>
   <%= image_tag 'no_image', size: "200x200" %>
  <% end %>
  <div class="list-comment">
   <span class="list-body"><%=link.body%></span>
   <span class="list-title"><%=list_to list.title, list_path(list.id)%></span>
  </div>
 </div>
 <%end%>
</div>